import{useQuery}from"@tanstack/react-query";import{router}from"expo-router";import{FlatList,Pressable,StyleSheet,Text,View}from"react-native";import{get}from"../../api/client";import{NavHeader}from"../../components/form-screen";import{Card,EmptyState,ErrorState,LoadingState,MoneyValue,Screen,StatusBadge}from"../../components/ui";import{colors,spacing}from"../../theme";import type{Quote}from"../../types/api";
export default function Quotes(){const q=useQuery({queryKey:["quotes"],queryFn:()=>get<Quote[]>("/quotes")});if(q.isLoading)return <LoadingState/>;if(q.isError)return <ErrorState message={q.error.message} retry={q.refetch}/>;return <Screen><NavHeader title="Quotes"/><FlatList data={q.data} keyExtractor={x=>x.id} contentContainerStyle={styles.list} ListEmptyComponent={<EmptyState title="No quotes" message="Quotes created on the web will appear here."/>} renderItem={({item})=><Pressable onPress={()=>router.push(`/quotes/${item.id}`)}><Card style={styles.card}><View style={{flex:1}}><Text style={styles.name}>{item.quoteNumber}</Text><Text style={styles.meta}>{item.customer.firstName} {item.customer.lastName} · {item.title}</Text></View><View style={styles.right}><MoneyValue value={item.totalAmount}/><StatusBadge status={item.status}/></View></Card></Pressable>}/></Screen>};const styles=StyleSheet.create({list:{padding:spacing.md},card:{marginBottom:spacing.sm,flexDirection:"row",gap:spacing.sm},name:{fontWeight:"900",color:colors.text},meta:{color:colors.muted,marginTop:4},right:{alignItems:"flex-end",gap:6}});
