import{useQuery}from"@tanstack/react-query";import{router}from"expo-router";import{FlatList,Pressable,RefreshControl,StyleSheet,Text}from"react-native";import{get}from"../../api/client";import{NavHeader}from"../../components/form-screen";import{Card,EmptyState,ErrorState,LoadingState,Screen}from"../../components/ui";import{colors,spacing}from"../../theme";import type{Subcontractor}from"../../types/api";
export default function Subcontractors(){const q=useQuery({queryKey:["subcontractors"],queryFn:()=>get<Subcontractor[]>("/subcontractors")});if(q.isLoading)return <LoadingState/>;if(q.isError)return <ErrorState message={q.error.message} retry={q.refetch}/>;return <Screen><NavHeader title="Subcontractors"/><FlatList data={q.data} keyExtractor={x=>x.id} refreshControl={<RefreshControl refreshing={q.isRefetching} onRefresh={q.refetch}/>} contentContainerStyle={styles.list} ListEmptyComponent={<EmptyState title="No subcontractors" message="Subcontractors added on the web will appear here."/>} renderItem={({item})=><Pressable onPress={()=>router.push(`/subcontractors/${item.id}`)}><Card style={styles.card}><Text style={styles.name}>{item.firstName} {item.lastName}</Text><Text style={styles.meta}>{item.businessName??item.email??item.phone??"No contact details"}</Text>{item.abn?<Text style={styles.meta}>ABN {item.abn}</Text>:null}</Card></Pressable>}/></Screen>};const styles=StyleSheet.create({list:{padding:spacing.md},card:{marginBottom:spacing.sm,gap:4},name:{fontWeight:"900",fontSize:17,color:colors.text},meta:{color:colors.muted}});
