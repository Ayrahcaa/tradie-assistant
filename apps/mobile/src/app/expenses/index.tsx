import{useQuery}from"@tanstack/react-query";import{router}from"expo-router";import{Plus}from"lucide-react-native";import{FlatList,Pressable,RefreshControl,StyleSheet,Text,View}from"react-native";import{get}from"../../api/client";import{NavHeader}from"../../components/form-screen";import{Card,EmptyState,ErrorState,LoadingState,MoneyValue,Screen,StatusBadge}from"../../components/ui";import{colors,spacing}from"../../theme";import type{Expense}from"../../types/api";
export default function Expenses(){const q=useQuery({queryKey:["expenses"],queryFn:()=>get<Expense[]>("/expenses")});if(q.isLoading)return <LoadingState/>;if(q.isError)return <ErrorState message={q.error.message} retry={q.refetch}/>;return <Screen><NavHeader title="Expenses"/><FlatList data={q.data} keyExtractor={x=>x.id} refreshControl={<RefreshControl refreshing={q.isRefetching} onRefresh={q.refetch}/>} contentContainerStyle={{paddingBottom:90,paddingTop:spacing.md}} ListEmptyComponent={<EmptyState title="No expenses" message="Capture a cost or receipt from the field."/>} renderItem={({item})=><Pressable onPress={()=>router.push(`/expenses/${item.id}`)}><Card style={styles.card}><View style={{flex:1}}><Text style={styles.name}>{item.description}</Text><Text style={styles.meta}>{item.supplier??item.category} · {new Date(item.expenseDate).toLocaleDateString("en-AU")}</Text></View><View style={styles.right}><MoneyValue value={item.amount}/><StatusBadge status={item.status}/></View></Card></Pressable>}/><Pressable style={styles.fab} onPress={()=>router.push("/expenses/new")}><Plus color={colors.text}/></Pressable></Screen>};const styles=StyleSheet.create({card:{marginHorizontal:spacing.md,marginBottom:spacing.sm,flexDirection:"row",gap:spacing.sm},name:{fontWeight:"900",fontSize:16,color:colors.text},meta:{color:colors.muted,marginTop:4},right:{alignItems:"flex-end",gap:7},fab:{position:"absolute",right:20,bottom:20,width:58,height:58,borderRadius:29,alignItems:"center",justifyContent:"center",backgroundColor:colors.primary}});
