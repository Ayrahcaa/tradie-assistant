import{useMutation,useQuery,useQueryClient}from"@tanstack/react-query";import{router}from"expo-router";import{useState}from"react";import{KeyboardAvoidingView,Platform,Pressable,ScrollView,StyleSheet,Text}from"react-native";import{get,post}from"../../api/client";import{NavHeader}from"../../components/form-screen";import{AppButton,AppInput,Card,Screen}from"../../components/ui";import{colors,spacing}from"../../theme";import type{Expense,Project}from"../../types/api";
const categories=["MATERIALS","TOOLS","FUEL","VEHICLE","SUBCONTRACTOR","EQUIPMENT_HIRE","INSURANCE","PHONE_INTERNET","OFFICE","TRAVEL","OTHER"];
export default function NewExpense(){const qc=useQueryClient();const projects=useQuery({queryKey:["projects"],queryFn:()=>get<Project[]>("/projects")});const[description,setDescription]=useState("");const[supplier,setSupplier]=useState("");const[amount,setAmount]=useState("");const[gst,setGst]=useState("");const[category,setCategory]=useState("MATERIALS");const[projectId,setProject]=useState<string|null>(null);const[notes,setNotes]=useState("");const m=useMutation({mutationFn:()=>post<Expense>("/expenses",{description,supplier:supplier||null,category,status:"PAID",amount:Number(amount),gstAmount:Number(gst||0),expenseDate:new Date().toISOString(),projectId,notes:notes||null}),onSuccess:async e=>{await qc.invalidateQueries({queryKey:["expenses"]});router.replace(`/expenses/${e.id}`)}});return <Screen><NavHeader title="New expense"/><KeyboardAvoidingView style={{flex:1}} behavior={Platform.OS==="ios"?"padding":undefined}><ScrollView keyboardShouldPersistTaps="handled" contentContainerStyle={styles.content}><Card style={styles.form}><AppInput label="Description" value={description} onChangeText={setDescription}/><AppInput label="Supplier" value={supplier} onChangeText={setSupplier}/><Text style={styles.label}>Category</Text><ScrollView horizontal showsHorizontalScrollIndicator={false}>{categories.map(x=><Pressable key={x} onPress={()=>setCategory(x)} style={[styles.chip,category===x&&styles.selected]}><Text style={styles.chipText}>{x.replaceAll("_"," ")}</Text></Pressable>)}</ScrollView><AppInput label="Amount (inc. GST)" value={amount} onChangeText={setAmount} keyboardType="decimal-pad"/><AppInput label="GST amount" value={gst} onChangeText={setGst} keyboardType="decimal-pad"/><Text style={styles.label}>Project (optional)</Text><ScrollView horizontal showsHorizontalScrollIndicator={false}>{projects.data?.map(x=><Pressable key={x.id} onPress={()=>setProject(projectId===x.id?null:x.id)} style={[styles.chip,projectId===x.id&&styles.selected]}><Text style={styles.chipText}>{x.name}</Text></Pressable>)}</ScrollView><AppInput label="Notes" value={notes} onChangeText={setNotes} multiline/>{m.error?<Text style={styles.error}>{m.error.message}</Text>:null}<AppButton title="Save expense" loading={m.isPending} disabled={description.trim().length<2||!(Number(amount)>0)} onPress={()=>m.mutate()}/></Card></ScrollView></KeyboardAvoidingView></Screen>};const styles=StyleSheet.create({content:{padding:spacing.md},form:{gap:spacing.md},label:{fontWeight:"700",color:colors.text},chip:{padding:11,borderRadius:99,backgroundColor:colors.surfaceAlt,marginRight:8},selected:{backgroundColor:"#FEF3C7",borderWidth:1,borderColor:colors.primary},chipText:{fontWeight:"700",fontSize:12,color:colors.text},error:{color:colors.danger}});
