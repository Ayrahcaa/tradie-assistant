import { Link } from "expo-router";
import { useState } from "react";
import { KeyboardAvoidingView, Platform, ScrollView, StyleSheet, Text, View } from "react-native";
import { useAuth } from "../../auth/auth-context";
import { AppButton, AppInput, Card, Screen } from "../../components/ui";
import { colors, spacing } from "../../theme";
export default function Login() { const { login } = useAuth(); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [busy,setBusy]=useState(false); async function submit(){setBusy(true);setError("");try{await login({email:email.trim().toLowerCase(),password});}catch(e){setError(e instanceof Error?e.message:"Unable to log in.");}finally{setBusy(false);}} return <Screen><KeyboardAvoidingView behavior={Platform.OS==="ios"?"padding":undefined} style={{flex:1}}><ScrollView keyboardShouldPersistTaps="handled" contentContainerStyle={styles.content}><View><Text style={styles.brand}>Tradie Assistant</Text><Text style={styles.tagline}>Your business, sorted — wherever the job takes you.</Text></View><Card style={styles.form}><Text style={styles.heading}>Welcome back</Text><AppInput label="Email" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" autoComplete="email"/><AppInput label="Password" value={password} onChangeText={setPassword} secureTextEntry autoComplete="current-password"/>{error?<Text style={styles.error}>{error}</Text>:null}<AppButton title="Log in" loading={busy} onPress={submit}/><Text style={styles.center}>New here? <Link href="/(auth)/register" style={styles.link}>Create an account</Link></Text></Card></ScrollView></KeyboardAvoidingView></Screen> }
const styles=StyleSheet.create({content:{flexGrow:1,justifyContent:"center",padding:spacing.lg,gap:spacing.xl},brand:{fontSize:34,fontWeight:"900",color:colors.text},tagline:{color:colors.muted,fontSize:16,marginTop:6},form:{gap:spacing.md},heading:{fontSize:23,fontWeight:"900",color:colors.text},error:{color:colors.danger},center:{textAlign:"center",color:colors.muted},link:{color:colors.primaryDark,fontWeight:"800"}});
