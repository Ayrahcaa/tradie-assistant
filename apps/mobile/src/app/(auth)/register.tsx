import { Link } from "expo-router"; import { useState } from "react"; import { KeyboardAvoidingView,Platform,ScrollView,StyleSheet,Text } from "react-native"; import { useAuth } from "../../auth/auth-context"; import { AppButton,AppInput,Card,Header,Screen } from "../../components/ui"; import { colors,spacing } from "../../theme";
export default function Register(){const{register}=useAuth();const[firstName,setFirst]=useState("");const[lastName,setLast]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(){if(password!==confirm){setError("Passwords do not match.");return;}setBusy(true);setError("");try{await register({firstName:firstName.trim(),lastName:lastName.trim(),email:email.trim().toLowerCase(),password});}catch(e){setError(e instanceof Error?e.message:"Unable to register.");}finally{setBusy(false)}}return <Screen><KeyboardAvoidingView behavior={Platform.OS==="ios"?"padding":undefined} style={{flex:1}}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><Header title="Create account" subtitle="Start managing your jobs from the field."/><Card style={styles.form}><AppInput label="First name" value={firstName} onChangeText={setFirst}/><AppInput label="Last name" value={lastName} onChangeText={setLast}/><AppInput label="Email" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address"/><AppInput label="Password" value={password} onChangeText={setPassword} secureTextEntry/><AppInput label="Confirm password" value={confirm} onChangeText={setConfirm} secureTextEntry/>{error?<Text style={styles.error}>{error}</Text>:null}<AppButton title="Create account" loading={busy} onPress={submit}/><Text style={styles.center}>Already registered? <Link href="/(auth)/login" style={styles.link}>Log in</Link></Text></Card></ScrollView></KeyboardAvoidingView></Screen>}; const styles=StyleSheet.create({content:{padding:spacing.md,paddingBottom:40},form:{gap:spacing.md},error:{color:colors.danger},center:{textAlign:"center",color:colors.muted},link:{color:colors.primaryDark,fontWeight:"800"}});
